{
  "name": "alarm",
  "version": "1.0.0",
  "description": "A simple alarm",
  "main": "index.js",
  "author": "Thomas Crescenzi",
  "license": "MIT",
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "dev": "yarn build && http-server",
    "start": "yarn build && http-server -p $PORT"
  },
  "dependencies": {
    "css-loader": "^0.28.7",
    "http-server": "^0.10.0",
    "style-loader": "^0.18.2",
    "webpack": "^3.5.6"
  },
  "engines": {
    "node": "^8"
  }
}
